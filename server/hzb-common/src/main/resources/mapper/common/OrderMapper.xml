<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzb.erp.common.mapper.OrderMapper">

    <select id="getList" resultType="com.hzb.erp.common.pojo.vo.OrderVo">
        select
            t1.id,t1.sn,t1.pay_money,t1.remark,t1.refunded,t1.state,t1.pay_time,t1.add_time,
            t2.name studentName
        from `order` t1
        left join student t2 on t2.id = t1.student_id
        <where>
            <if test="param.studentId != null">
                AND t1.student_id = #{param.studentId}
            </if>
            <if test="param.state != null">
                AND t1.state = #{param.state}
            </if>
            <if test="param.sn != null and param.sn.trim() != ''">
                AND t1.sn like concat( #{param.sn}, '%')
            </if>
            and t1.deleted = 0
        </where>
        order by t1.id desc
    </select>

</mapper>
